 
name: yir_text_form_field
description: A Flutter text form field with glassmorphism design, animated borders, shimmer effects, blur background, and fully customizable UI.
version: 0.0.1

homepage: https://github.com/Younis7575/yir_text_form_field
repository: https://github.com/Younis7575/yir_text_form_field
issue_tracker: https://github.com/Younis7575/yir_text_form_field/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# 🔥 Package discoverability improve karega
topics:
  - flutter
  - ui
  - glassmorphism
  - widget
  - design

# 🔥 Platform support (auto detect hota hai but explicit better)
platforms:
  android:
  ios:
  web:
  windows:
  macos:
  linux:

flutter: